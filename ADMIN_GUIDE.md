# 信息分享平台后台使用说明

当前后台是第一阶段的“本地管理后台”：

1. 打开 `admin.html`；
2. 新建、编辑或删除内容；
3. 点击“保存到本地”；
4. 回到前台 `index.html` 并刷新，即可看到当前浏览器里的改动；
5. 要发布给所有访客，点击“导出 content.json”，把导出的文件替换到 `data/content.json`。

## 自定义板块

后台“板块设置”支持：

- 新增、重命名和删除板块；
- 上下调整导航顺序；
- 启用或停用板块；
- 设置板块颜色和 Lucide 图标；
- 设置中英文自动分类关键词；
- 重命名时自动迁移已有文章；
- 删除时将原内容转移到其他板块。

新增板块后，后台智能生成会立即使用其关键词。要让 GitHub 自动采集脚本也使用新板块，请导出最新 `content.json` 并替换 `data/content.json`。

## 站点外观

后台“站点外观”可以修改：

- 平台名称和英文副标；
- 首页主标题、说明和页脚；
- 页面背景、面板、文字、主科技色和辅助科技色。

## 高级配置

“高级 JSON 配置”可以编辑全部数据，包括首页指标、趋势图、推荐内容、热词、活动日历、板块和文章。点击“校验并应用”后，只有合法 JSON 才会保存。

## 智能更新

运行：

```bash
python scripts/auto_update.py
```

脚本会生成：

```text
data/intelligence-draft.json
```

然后进入后台“智能更新”，点击“导入智能草稿 JSON”，选择这个文件，审核后保存。

也可以点击“同步最新自动草稿”，直接读取 `data/intelligence-draft.json`。

### 来源、去重和日志

- 在“智能更新”里的“采集来源管理”可以新增、停用或删除 RSS / Atom、官方网页栏目和官方 JSON 来源，并设置默认板块、可信度和单次条数；
- 本地模式修改来源后，导出最新 `content.json` 到 `data/content.json`，采集脚本会优先读取其中的 `sourceSettings`；
- 自动采集会同时按规范化来源链接和标题指纹去重，已有正式内容与尚未审核的草稿都不会重复生成；
- `data/collection-logs.json` 保留最近 50 次采集记录，后台会显示新增、重复、失败来源和错误原因；
- 默认来源清单位于 `data/update-sources.json`。当前优先覆盖央行、政府部门、科研机构、大学和行业协会；文章详情页会公开机构类型、地区、可信等级、原文发布时间和采集时间；
- 每日任务优先使用 GitHub Models 的免费额度，把来源摘要和有限原文片段整理为中文科普文章；模型临时不可用时切换到不依赖模型的来源约束结构化整理。每个板块每日自动发布目标不得低于 2 篇，当前设为 3 篇；自动公开正文必须达到 800 字、至少 6 个不重复段落并保留来源说明；不达标内容只进入待审核区；
- 文章详情页提供“查看完整原文”入口。除非来源明确给出可转载授权，平台只发布中文整理，不复制第三方全文；
- 每次生成的新内容仍是草稿，必须在后台审核后发布。

## 视频资料

文章编辑表单支持 HTTPS 的 MP4 / WebM 直连视频、哔哩哔哩官方嵌入和外部观看链接。配置视频后必须确认该视频允许嵌入或传播，否则文章不能通过发布质量检查。未知平台不会直接嵌入，只在文章页提供“前往来源网站观看”入口；未配置视频的文章仍正常显示封面图片。

每日智能更新已接入中央广播电视总台《创新进行时》官方栏目接口。最新 3 条视频使用独立正文生成名额，不与各板块的普通文章名额冲突；新视频会生成带封面、中文科普正文、时长和央视原始播放页的文章，并进入首页视频资料区。平台只链接央视官方播放页，不下载、不转存、不重新上传视频；接口临时失败时会保留已有内容，不会清空首页视频。

项目已经包含 `.github/workflows/smart-update.yml`。上传到 GitHub 后，它会每天北京时间 08:17 左右自动执行采集脚本并更新草稿文件，也可以在 GitHub Actions 页面手动执行。选择 08:17 是为了避开 GitHub 整点调度高峰，平台允许少量运行延迟。

配置 GitHub 加密密钥 `SUPABASE_SERVICE_ROLE_KEY` 后，每日任务还会自动把新增内容追加到 Supabase 待审核区。它不会覆盖已有文章，也不会绕过人工审核发布。详细配置见 `SUPABASE_SETUP.md` 的“自动写入 Supabase 待审核区”。

## 搜索、SEO 与备份

- 前台搜索支持标题、摘要、正文、作者、来源和标签，可用空格组合多个关键词；搜索与板块会保留在网址参数中；
- 正式域名在“站点外观”里的“公益运营与站点信息”设置，首页与文章页会据此生成 canonical、Open Graph 和结构化数据；
- 更新 `data/content.json` 后运行 `python scripts/generate_sitemap.py`，会重新生成只包含公开内容的 `sitemap.xml`；
- 后台每次保存前会自动创建去重快照，默认保留 10 份；也可在“发布上线”中手动创建、下载或恢复；
- 浏览器快照不替代异地备份。上线后仍建议保留 GitHub 仓库历史和 Supabase 定期导出。

## 公开联系渠道

治理页的在线表单是默认公开联系渠道，可接收纠错、版权、隐私、下架和其他运营事项，并写入 Supabase 后台反馈队列。后台“运营与合规”里的公开邮箱为可选项；在没有专用运营邮箱前建议留空，不要公开个人常用邮箱。

## 正式上线后台建议

浏览器静态页面不能安全地直接写服务器文件。后续建议升级为：

- Cloudflare Pages：部署前台；
- GitHub Actions：定时运行智能更新脚本；
- Supabase：登录、数据库、文件存储；
- Cloudflare Access：给 `/admin` 加管理员登录保护。

桌面版本已经加入 Supabase 登录、云数据库和云文件上传适配。详细配置顺序见 `SUPABASE_SETUP.md`。
